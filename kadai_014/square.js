function calculateSquare(number) {
    
    const square = number * number;
  
        return square;
  }
  
  // 関数を実行して結果をコンソールに出力
  const result = calculateSquare(10);
  console.log(result); 
